# AutoBangumi Domain

AutoBangumi 将发布内容关联到番剧规则，并把媒体文件整理为媒体库可识别的结构。本文统一描述下载、解析与重命名流程中的领域术语。

## Language

**自动解析集数（Automatically Parsed Episode）**:
解析器从单个发布标题或媒体文件名中识别出的原始集数，不包含人工纠错或番剧规则的集数偏移。
_Avoid_: 解析集数、最终集数

**解析集数（Parsed Episode）**:
媒体文件当前采用的原始集数；存在单集纠错时取人工值，否则取自动解析集数，不包含番剧规则的集数偏移。
_Avoid_: 自动解析集数、最终集数

**集数偏移（Episode Offset）**:
番剧规则提供的集数修正量，适用于该规则所匹配的发布内容，并且独立于解析集数。
_Avoid_: 手动集数、单集修正

**最终集数（Effective Episode）**:
媒体文件重命名和入库时所属的集数，等于解析集数加集数偏移。
_Avoid_: 解析集数

**单集纠错（Episode Correction）**:
针对一个媒体文件持久覆盖一个或多个命名字段的人工修正，不改变番剧规则；它优先于后续自动解析，直到用户恢复缺省值，与该视频匹配的字幕跟随修正后的最终名称。
_Avoid_: 集数偏移、规则修正

**合集种子（Collection Torrent）**:
包含多个视频文件的下载任务；每个视频文件拥有各自的解析集数和单集纠错。
_Avoid_: 单集任务

**番剧分组（Bangumi Group）**:
具有稳定身份的媒体条目，可包含多个不同来源的订阅规则；创建或迁移时可按正式标题、年份、季度和类型自动归组，之后元数据编辑不改变成员归属。
_Avoid_: 订阅规则、单个 Bangumi ID

**成员规则（Member Rule）**:
归属于一个番剧分组的订阅规则，独立拥有原始匹配标题、发布来源、过滤条件和季度/集数偏移；番剧分组的剧集视图合并展示所有成员规则关联的下载任务，并保留每个任务的来源规则。
_Avoid_: 番剧分组

**分组元数据（Group Metadata）**:
由番剧分组统一拥有的正式标题、年份、季度、类型、海报和放送信息；所有成员规则和命名计划共享这些值。
_Avoid_: 规则条件、逐文件字段

**命名字段（Naming Field）**:
组合媒体文件最终名称所使用的一项信息，例如标题、年份、季度、集数或字幕组。
_Avoid_: 整理状态、下载状态

**逐文件字段（Per-file Field）**:
从单个媒体文件名解析、并被当前命名规则用于组合最终名称的命名字段；它允许通过单集纠错覆盖，而来自番剧、规则或下载任务的字段不属于逐文件字段。
_Avoid_: 番剧元数据、规则字段

**字段来源（Field Source）**:
命名字段当前值的来源，仅使用“缺省”和“手动”两个值；缺省值由解析器或番剧元数据提供，人工修改的值持续覆盖缺省值。
_Avoid_: 自定义、重命名状态

**缺省解析快照（Default Parse Snapshot）**:
媒体文件首次生成命名计划时保存的逐文件自动解析结果；解析器升级不会静默改变它，只有用户明确重新解析时才会更新，且不会覆盖手动字段。
_Avoid_: 实时解析结果、手动字段

**计划修订（Plan Revision）**:
命名字段、分组元数据、规则值或命名模板变化后形成的新目标名称版本；尚未执行的计划使用最新修订，已经执行过的文件必须由用户明确应用新修订。
_Avoid_: 自动批量重命名

**已确认修订（Approved Plan Revision）**:
用户已经明确要求应用的计划修订；它先持久保存再立即执行，暂时失败时可在进程重启后继续重试，直到完成或出现需要用户处理的异常。
_Avoid_: 表单草稿、未确认预览

**命名异常（Naming Anomaly）**:
当前文件夹或文件命名规则引用的字段缺失，或这些字段无法组合出安全有效的最终名称；异常按媒体文件隔离并阻止该视频及匹配字幕自动应用，未被当前规则引用的字段缺失不构成异常。
_Avoid_: 下载状态、字段来源

**命名单元（Naming Unit）**:
一个视频文件及其匹配字幕组成的最小应用与恢复边界；应用前整体预检，执行中逐文件记录进度，部分失败时保留已完成结果并继续向前重试。
_Avoid_: 合集种子、数据库事务

**命名计划（Naming Plan）**:
一个真实媒体文件的命名字段、字段来源以及由当前命名规则组合出的最终名称预览；下载器首次提供文件列表时生成，不因下载尚未完成而延迟，元数据未就绪时不创建虚假计划。下载任务暂时消失时计划继续存在，同内容任务重新出现时恢复关联，只有用户明确删除 AutoBangumi 下载记录时才随之删除。
_Avoid_: 下载任务、订阅规则

**媒体文件身份（Media File Identity）**:
由 AutoBangumi 内部下载任务身份与下载器文件序号共同确定的稳定身份；原始路径、当前路径和目标路径都是可变属性，不用于判断是否为同一文件。
_Avoid_: 文件路径、文件名

**既有命名基线（Legacy Naming Baseline）**:
升级时从下载器观测到的当前文件路径，只表示回填开始时的实际状态；它既不是手动纠错，也不证明文件已经或尚未由 AutoBangumi 重命名，升级不会仅因生成该基线而改动文件。
_Avoid_: 缺省解析快照、已确认修订

**执行租约（Execution Lease）**:
执行器通过数据库条件更新为一个命名单元领取的有时限独占执行权，包含已确认修订号和栅栏令牌；所有自动、立即和重试入口共用它，过期持有者不能继续推进状态。
_Avoid_: 数据库事务、下载器锁

**字幕关联（Subtitle Association）**:
同一下载任务中一个字幕文件到唯一视频文件的持久关系；只有类型、季度、最终集数或唯一文件主干能够证明时自动建立，歧义保持未关联并允许用户明确指定。
_Avoid_: 文件列表顺序、字幕语言
